are these the same structural build? if so, can you combine them?

"""
===============================================================================
SYSTEM ARCHITECTURE HEADER: DETERMINISTIC INTEGRITY TOWER (DIT)
===============================================================================
Version-Control-ID: [SHA256-PLACEHOLDER-HASH-DIT-GOVERNANCE-2026]

PURPOSE AND ROLE:
This system provides an analytical and operational runtime governance layer designed
to manage risky processing tasks (like financial risk models) inside a highly secure, 
auditable state ledger. It wraps standard modules using a cryptographic interlock pattern 
to ensure that all operational processing is traceable, linear, cyclical, or synchronized 
against real-time cryptographic states. This prevents unexpected deviations, system 
vulnerabilities, and black-box operational drift.

ARCHITECTURE COMPONENTS:
1. GSA Universal Cryptographic Interlock Wrapper Engine: Enforces strict state validation,
   tracking system execution via cryptographic hashing history.
2. Defensive Operational Intelligence System (DOIS): A core reference risk-evaluation
   engine containing specialized models to flag traditional, behavioral, and adversarial
   borrower risk attributes.
3. GsaTemporalDoorwayGate: Provides real-time synchronization safety hooks to match execution 
   states with time-dilated entropy seeds before finalizing process output.
"""

# ===============================================================================
# STRUCTURAL INTEGRITY & REPAIR LOG
# ===============================================================================
# A comprehensive validation scan discovered and repaired the following items:
# 
# 1. UNDEFINED LOCAL VARIABLE IN WRAPPER EXIT BRANCH:
#    - Problem: In `GsaUniversalAdapter.process_payload`, the `else` branch for tracking 
#      un-anchored states referenced an undefined local variable `outbound_linear_hash`.
#    - Fix: Refactored logic to use the computed `outbound_hash` as the baseline metric.
# 
# 2. SEPARATED RUNTIME IMPLEMENTATION ASSETS:
#    - Problem: The `DefensiveOperationalIntelligenceSystem` components and domain models
#      were separate objects lacking an operational bridge to the `GsaUniversalAdapter` pipeline.
#    - Fix: Integrated mock structural objects for `context_envelope` (`GsaEnvelope`) and a 
#      mock implementation of `deep_freeze_structure_function` to make the script completely
#      executable out-of-the-box.
#
# 3. TYPE HOOK SYNCHRONIZATION:
#    - Problem: Properties used in the DOIS orchestrator lacked standard data container models 
#      compatible with the wrapper mapping expectation (`header_mapping`, `payload_data`, etc.).
#    - Fix: Included an explicit adapter method within DOIS to natively map domain payloads.
# ===============================================================================

from __future__ import annotations

import asyncio
import copy
import hashlib
import json
import random
import statistics
import time
from dataclasses import dataclass, field, replace
from types import MappingProxyType
from typing import Any, Callable, Dict, List, Mapping, Optional, Protocol, Tuple, Union

# ============================================================
# RUNTIME FOUNDATION SHIMS (SOPHOMORE-LEVEL FALLBACKS)
# ============================================================

def deep_freeze_structure_function(d: Dict[str, Any]) -> MappingProxyType:
    """
    Simulates a deep freeze function to maintain cryptographic consistency.
    Wraps dictionary metadata in a read-only MappingProxyType structure.
    """
    return MappingProxyType(copy.deepcopy(d))


@dataclass(frozen=True)
class GsaEnvelope:
    """Unified data container envelope expected by the GSA Universal Interlock Wrapper."""
    payload_data: Dict[str, Any] = field(default_factory=dict)
    session_state_mapping: Dict[str, Any] = field(default_factory=dict)
    header_mapping: MappingProxyType = field(default_factory=lambda: MappingProxyType({}))
    status_string: str = "INITIALIZED"


# ============================================================
# PROTOCOLS & CORE COMPLIANCE INTERFACES
# ============================================================

class ComposableLegoModule(Protocol):
    """Defines the unified asynchronous footprint required for all GSA system components."""
    async def process_payload(self, context_envelope: Any) -> Any:
        ...


# ============================================================
# CRYPTOGRAPHIC DETERMINISTIC STATE CALCULATION UTILITIES
# ============================================================

def compute_state_signature(
    upstream_hash: str, 
    iteration: int, 
    envelope: Any, 
    extra_anchors: Optional[List[str]] = None
) -> str:
    """
    Computes a deterministic SHA-256 block hash incorporating the linear history,
    iteration sequences, graph convergence arrays, payload data, and state schemas.
    """
    serialized_payload = json.dumps(envelope.payload_data, sort_keys=True, default=str)
    serialized_session = json.dumps(envelope.session_state_mapping, sort_keys=True, default=str)
    
    # Process extra anchors deterministically if merging a graph split
    sorted_anchors = "||".join(sorted(extra_anchors)) if extra_anchors else "NONE"
    
    buffer_source = (
        f"parent:{upstream_hash}||"
        f"iter:{iteration}||"
        f"graph:[{sorted_anchors}]||"
        f"payload:{serialized_payload}||"
        f"session:{serialized_session}"
    )
    
    return hashlib.sha256(buffer_source.encode("utf-8")).hexdigest()


# ============================================================
# UNIVERSAL CRYPTOGRAPHIC ADAPTER (THE WRAPPER ENGINE)
# ============================================================

class GsaUniversalAdapter:
    """
    The Universal Adapter wrapper. Encloses any synchronous or asynchronous GSA module,
    enforcing linear, cyclical, fork-join, static anchor, and temporal doorway controls.
    """
    def __init__(
        self, 
        underlying_module: Any, 
        translation_bridge: Optional[Callable[[Any, Any], Any]] = None
    ) -> None:
        self.module = underlying_module
        self.bridge = translation_bridge or (lambda m, env: env)
        self.actor_name = type(underlying_module).__name__

    async def process_payload(self, context_envelope: Any) -> Any:
        """Processes the envelope data layer, continuously managing the structural state ledger."""
        headers = dict(context_envelope.header_mapping)
        hash_history = list(headers.get("gsa_chain_history", []))
        fork_tracking = dict(headers.get("gsa_graph_forks", {}))
        anchor_registry = dict(headers.get("gsa_static_anchors", {}))
        
        current_iteration = headers.get("gsa_loop_iteration", 0)
        reentry_target_id = headers.get("gsa_reentry_target_id")
        
        upstream_hash = "GENESIS_ANCHOR"
        target_merge_keys: List[str] = []
        upstream_anchors: List[str] = []

        # --------------------------------------------------------
        # PHASE 1: INBOUND VERIFICATION & ROUTING
        # --------------------------------------------------------
        if reentry_target_id and reentry_target_id in anchor_registry:
            saved_anchor_hash = anchor_registry[reentry_target_id]
            provided_current_hash = headers.get("gsa_interlock_hash")
            
            if provided_current_hash != saved_anchor_hash:
                return replace(
                    context_envelope,
                    status_string=f"GSA_ANCHOR_MISMATCH: Deviation identified for anchor '{reentry_target_id}'."
                )
            
            headers.pop("gsa_reentry_target_id", None)  # Consume re-entry trigger
            upstream_hash = saved_anchor_hash

        else:
            target_merge_keys = [k for k, v in fork_tracking.items() if v == self.actor_name]
            if target_merge_keys:
                upstream_anchors = [headers.get(f"gsa_branch_hash_{k}", "") for k in target_merge_keys]
                upstream_hash = "||".join(upstream_anchors)
                
                # Prune branch identifiers from metadata track upon convergence
                for k in target_merge_keys:
                    fork_tracking.pop(k, None)
                    headers.pop(f"gsa_branch_hash_{k}", None)
            else:
                upstream_hash = hash_history[-1] if hash_history else "GENESIS_ANCHOR"
                
                if hash_history:
                    provided_current_hash = headers.get("gsa_interlock_hash")
                    prior_anchor = hash_history[-2] if len(hash_history) > 1 else "GENESIS_ANCHOR"
                    expected_current_hash = compute_state_signature(prior_anchor, current_iteration, context_envelope)
                    
                    if provided_current_hash != expected_current_hash:
                        return replace(
                            context_envelope,
                            status_string=f"GSA_CHAIN_BREAK: Signature validation failed at iteration {current_iteration}."
                        )

        # Update tracking context variables inside envelope headers prior to code execution
        headers["gsa_graph_forks"] = fork_tracking
        working_envelope = replace(context_envelope, header_mapping=deep_freeze_structure_function(headers))

        # --------------------------------------------------------
        # PHASE 2: MODULE LOGIC EXECUTION OVER INTERFACE BOUNDARY
        # --------------------------------------------------------
        if hasattr(self.module, "execute_governance_logic"):
            output_envelope = await self.module.execute_governance_logic(working_envelope)
        elif hasattr(self.module, "execute_governance_module"):
            output_envelope = await self.module.execute_governance_module(working_envelope)
        else:
            # Handle standard synchronous fallback tasks via running event loops
            loop = asyncio.get_event_loop()
            output_envelope = await loop.run_in_executor(None, self.bridge, self.module, working_envelope)

        # --------------------------------------------------------
        # PHASE 3: OUTBOUND MATRICES STAMPING & LOCKING
        # --------------------------------------------------------
        updated_headers = dict(output_envelope.header_mapping)
        set_anchor_id = updated_headers.pop("gsa_set_static_anchor_id", None)
        
        # Increment execution index counter for looping support
        next_iteration = current_iteration + 1
        
        # Generate the signature for this execution stage
        outbound_hash = compute_state_signature(
            upstream_hash, 
            next_iteration, 
            output_envelope, 
            extra_anchors=upstream_anchors if target_merge_keys else None
        )
        hash_history.append(outbound_hash)

        # If static save-state was requested, add it to the anchor dictionary
        if set_anchor_id:
            anchor_registry[set_anchor_id] = outbound_hash
            updated_headers["gsa_interlock_hash"] = outbound_hash
        else:
            # FIX applied to prevent Runtime Reference Errors from missing local variables
            updated_headers["gsa_interlock_hash"] = outbound_hash

        # Synchronize updated metadata fields back into the tracking headers
        updated_headers["gsa_chain_history"] = hash_history
        updated_headers["gsa_static_anchors"] = anchor_registry
        updated_headers["gsa_loop_iteration"] = next_iteration
        updated_headers["gsa_last_actor"] = self.actor_name

        return replace(
            output_envelope,
            header_mapping=deep_freeze_structure_function(updated_headers)
        )


# ============================================================
# DEFENSIVE OPERATIONAL INTELLIGENCE SYSTEM (DOIS)
# ============================================================

@dataclass
class SystemConfig:
    stability_threshold: float = 0.15
    disagreement_alert_threshold: float = 0.30
    manipulation_penalty_weight: float = 0.40
    override_logging_enabled: bool = True
    random_seed: int = 42


@dataclass
class Borrower:
    borrower_id: str
    annual_income: float
    loan_balance: float
    credit_score: int
    monthly_payment_amount: float
    delinquency_status: str
    engagement_recency: str
    primary_channel: str
    loan_type: str
    save_tranche: str


@dataclass
class Prediction:
    model_name: str
    risk_score: float
    confidence: float
    explanation: str


@dataclass
class Decision:
    action: str
    confidence: float
    disagreement_score: float
    stability_score: float
    override_applied: bool
    explanation: str


class DistortionEngine:
    @staticmethod
    def estimate_signal_distortion(b: Borrower) -> float:
        score = 0.0
        if b.monthly_payment_amount == 0:
            score += 0.15
        if b.engagement_recency == "Never":
            score += 0.25
        if b.primary_channel == "IVR":
            score += 0.10
        if b.delinquency_status in ["60-89", "90+"]:
            score += 0.20
        if b.credit_score < 600:
            score += 0.10
        return min(score, 1.0)


class BaseModel:
    def __init__(self, name: str):
        self.name = name

    def predict(self, borrower: Borrower) -> Prediction:
        raise NotImplementedError


class CreditRiskModel(BaseModel):
    def __init__(self):
        super().__init__("CreditRiskModel")

    def predict(self, borrower: Borrower) -> Prediction:
        risk = 0.0
        if borrower.credit_score < 580:
            risk += 0.45
        elif borrower.credit_score < 650:
            risk += 0.30
        elif borrower.credit_score < 720:
            risk += 0.15
        if borrower.loan_balance > 70000:
            risk += 0.20
        if borrower.monthly_payment_amount == 0:
            risk += 0.15
        return Prediction(
            model_name=self.name,
            risk_score=min(risk, 1.0),
            confidence=0.72,
            explanation="Traditional credit profile analysis."
        )


class EngagementFragilityModel(BaseModel):
    def __init__(self):
        super().__init__("EngagementFragilityModel")

    def predict(self, borrower: Borrower) -> Prediction:
        risk = 0.0
        if borrower.engagement_recency == "Never":
            risk += 0.45
        elif borrower.engagement_recency == ">1yr":
            risk += 0.30
        elif borrower.engagement_recency == "1-12mo":
            risk += 0.15
        if borrower.primary_channel == "IVR":
            risk += 0.10
        if borrower.delinquency_status == "90+":
            risk += 0.25
        return Prediction(
            model_name=self.name,
            risk_score=min(risk, 1.0),
            confidence=0.69,
            explanation="Behavioral disengagement analysis."
        )


class AdversarialBehaviorModel(BaseModel):
    def __init__(self):
        super().__init__("AdversarialBehaviorModel")

    def predict(self, borrower: Borrower) -> Prediction:
        risk = 0.0
        if borrower.monthly_payment_amount == 0:
            risk += 0.25
        if borrower.primary_channel == "IVR":
            risk += 0.15
        if borrower.credit_score > 760 and borrower.delinquency_status in ["60-89", "90+"]:
            risk += 0.35
        if borrower.engagement_recency == "<30d":
            risk -= 0.10
        return Prediction(
            model_name=self.name,
            risk_score=max(min(risk, 1.0), 0.0),
            confidence=0.61,
            explanation="Strategic behavior detection."
        )


class EnsembleEngine:
    def __init__(self, config: SystemConfig):
        self.config = config

    @staticmethod
    def disagreement(predictions: List[Prediction]) -> float:
        values = [p.risk_score for p in predictions]
        if len(values) <= 1:
            return 0.0
        return statistics.pstdev(values)

    @staticmethod
    def weighted_consensus(predictions: List[Prediction], distortion_penalty: float) -> float:
        weighted_total = 0.0
        weight_sum = 0.0
        for p in predictions:
            robustness_weight = p.confidence * (1 - distortion_penalty)
            weighted_total += p.risk_score * robustness_weight
            weight_sum += robustness_weight
        if weight_sum == 0:
            return 0.5
        return weighted_total / weight_sum


class StabilityEngine:
    def __init__(self):
        self.previous_score: Optional[float] = None

    def stability_score(self, current_score: float) -> float:
        if self.previous_score is None:
            self.previous_score = current_score
            return 1.0
        delta = abs(current_score - self.previous_score)
        self.previous_score = current_score
        return max(0.0, 1 - delta)


class DecisionEngine:
    @staticmethod
    def determine_action(ensemble_score: float, disagreement: float, stability: float) -> Tuple[str, str]:
        if disagreement > 0.35:
            return ("ESCALATE_FOR_REVIEW", "High model disagreement detected.")
        if stability < 0.50:
            return ("HOLD_ACTION", "System instability detected.")
        if ensemble_score >= 0.65:
            return ("HIGH_TOUCH_OUTREACH", "Borrower considered high operational risk.")
        elif ensemble_score >= 0.40:
            return ("DIGITAL_NUDGE", "Moderate borrower fragility detected.")
        return ("MONITOR_ONLY", "Low operational risk profile.")


@dataclass
class OverrideRequest:
    actor: str
    replacement_action: str
    reason: str


class HumanOverrideLayer:
    def __init__(self):
        self.audit_log: List[Dict] = []

    def apply_override(self, decision: Decision, override: Optional[OverrideRequest]) -> Decision:
        if override is None:
            return decision

        self.audit_log.append({
            "actor": override.actor,
            "reason": override.reason,
            "original_action": decision.action,
            "replacement_action": override.replacement_action
        })

        return Decision(
            action=override.replacement_action,
            confidence=decision.confidence,
            disagreement_score=decision.disagreement_score,
            stability_score=decision.stability_score,
            override_applied=True,
            explanation=f"Human override by {override.actor}: {override.reason}"
        )


class DefensiveOperationalIntelligenceSystem:
    """Core framework orchestrating evaluation logic under risk mitigating protocols."""
    def __init__(self, config: SystemConfig):
        random.seed(config.random_seed)
        self.config = config
        self.models = [CreditRiskModel(), EngagementFragilityModel(), AdversarialBehaviorModel()]
        self.distortion_engine = DistortionEngine()
        self.ensemble_engine = EnsembleEngine(config)
        self.stability_engine = StabilityEngine()
        self.decision_engine = DecisionEngine()
        self.override_layer = HumanOverrideLayer()

    def evaluate(self, borrower: Borrower, override: Optional[OverrideRequest] = None) -> Decision:
        predictions = [model.predict(borrower) for model in self.models]
        distortion_score = self.distortion_engine.estimate_signal_distortion(borrower)
        ensemble_score = self.ensemble_engine.weighted_consensus(predictions, distortion_score)
        disagreement = self.ensemble_engine.disagreement(predictions)
        stability = self.stability_engine.stability_score(ensemble_score)
        
        action, explanation = self.decision_engine.determine_action(ensemble_score, disagreement, stability)

        decision = Decision(
            action=action,
            confidence=round(ensemble_score, 3),
            disagreement_score=round(disagreement, 3),
            stability_score=round(stability, 3),
            override_applied=False,
            explanation=explanation
        )
        return self.override_layer.apply_override(decision, override)

    async def execute_governance_logic(self, envelope: GsaEnvelope) -> GsaEnvelope:
        """Asynchronous execution hook matching the ComposableLegoModule expected schema."""
        borrower_dict = envelope.payload_data.get("borrower_data", {})
        borrower_obj = Borrower(**borrower_dict)
        
        override_dict = envelope.payload_data.get("override_request")
        override_obj = OverrideRequest(**override_dict) if override_dict else None
        
        # Perform inner engine processing
        raw_decision = self.evaluate(borrower_obj, override=override_obj)
        
        output_payload = {
            "action": raw_decision.action,
            "confidence": raw_decision.confidence,
            "disagreement_score": raw_decision.disagreement_score,
            "stability_score": raw_decision.stability_score,
            "override_applied": raw_decision.override_applied,
            "explanation": raw_decision.explanation
        }
        
        return replace(envelope, payload_data=output_payload, status_string="EXECUTION_COMPLETE")


# ============================================================
# STANDALONE EXIT DOORWAY MODULE (TEMPORAL INTERLOCK)
# ============================================================

class GsaTemporalDoorwayGate:
    """
    Standalone exit boundary module. Requires a spatial hash matching condition
    synchronized simultaneously with a rotating temporal high-precision seed.
    """
    def __init__(self, rotation_seed: str, rotation_interval_seconds: float = 0.05) -> None:
        self._seed = rotation_seed
        self._interval = rotation_interval_seconds
        self._current_doorway_hash = ""
        self._is_operating = False
        self._lock = asyncio.Lock()
        
    async def start_gate_engine(self) -> None:
        """Activates the isolated micro-loop driving continuous hash rotation."""
        self._is_operating = True
        asyncio.create_task(self._hash_rotation_worker())

    async def shutdown_gate_engine(self) -> None:
        """Deactivates the rotation thread cleanly."""
        self._is_operating = False

    async def _hash_rotation_worker(self) -> None:
        while self._is_operating:
            async with self._lock:
                entropy_buffer = f"{self._seed}||{time.time_ns()}".encode("utf-8")
                self._current_doorway_hash = hashlib.sha256(entropy_buffer).hexdigest()
            await asyncio.sleep(self._interval)

    async def execute_governance_logic(self, envelope: Any) -> Any:
        """Holds execution processing until the incoming hash aligns with the rotating gate signature."""
        headers = dict(envelope.header_mapping)
        target_exit_hash = headers.get("gsa_target_exit_hash")

        if not target_exit_hash:
            return replace(
                envelope,
                status_string="GSA_DOORWAY_REJECT: Exit configuration requires 'gsa_target_exit_hash'."
            )

        timeout_threshold = headers.get("gsa_doorway_timeout_seconds", 3.0)
        execution_start = time.time()
        handshake_secured = False

        while (time.time() - execution_start) < timeout_threshold:
            async with self._lock:
                if self._current_doorway_hash == target_exit_hash:
                    handshake_secured = True
                    break
            await asyncio.sleep(0.005)  # Minimize event loop context locking costs

        updated_headers = dict(envelope.header_mapping)

        if handshake_secured:
            updated_headers["gsa_doorway_cleared_hash"] = self._current_doorway_hash
            updated_headers["gsa_doorway_timestamp_ns"] = time.time_ns()
            return replace(
                envelope,
                status_string="GSA_EXIT_HANDSHAKE_COMPLETED",
                header_mapping=deep_freeze_structure_function(updated_headers)
            )
        else:
            return replace(
                envelope,
                status_string="GSA_DOORWAY_TIMEOUT: Temporal synchronization alignment window missed.",
                header_mapping=deep_freeze_structure_function(updated_headers)
            )


# ============================================================
# ORCHESTRATION PIPELINE PIPELINE EXECUTION
# ============================================================

async def main():
    # Setup test mock profiles
    borrower_payload = {
        "borrower_id": "9FDTT5Y",
        "annual_income": 49601,
        "loan_balance": 36023,
        "credit_score": 738,
        "monthly_payment_amount": 147,
        "delinquency_status": "Current",
        "engagement_recency": "1-12mo",
        "primary_channel": "Agent",
        "loan_type": "Consolidation",
        "save_tranche": "T7"
    }

    initial_envelope = GsaEnvelope(
        payload_data={"borrower_data": borrower_payload},
        header_mapping=deep_freeze_structure_function({
            "gsa_chain_history": [],
            "gsa_graph_forks": {},
            "gsa_static_anchors": {},
            "gsa_loop_iteration": 0
        })
    )

    # Initialize Core Modules
    config = SystemConfig()
    dois_engine = DefensiveOperationalIntelligenceSystem(config)
    adapter = GsaUniversalAdapter(underlying_module=dois_engine)

    print("Executing encapsulated DIT Pipeline Step...")
    final_envelope = await adapter.process_payload(initial_envelope)
    
    print("\n==============================================")
    print("INTERLOCK WRAPPER EXECUTION MATRIX OUTPUT")
    print("==============================================")
    print(f"Status State:   {final_envelope.status_string}")
    print(f"Assigned Hash:  {final_envelope.header_mapping.get('gsa_interlock_hash')}")
    print(f"Pipeline Step:  {final_envelope.header_mapping.get('gsa_last_actor')}")
    print(f"Payload Action: {final_envelope.payload_data.get('action')}")
    print(f"Explanation:    {final_envelope.payload_data.get('explanation')}")
    print("==============================================\n")

if __name__ == "__main__":
    asyncio.run(main())

# ===============================================================================
# GITHUB TRANSPORTABILITY & REPOSITORY HYGIENE (.gitignore STRUCTURE)
# ===============================================================================
# # Workspace tracking exclusion matrix for local development
# .target_runtime_cache/
# __pycache__/
# *.py[cod]
# *$py.class
# .env
# .venv
# env/
# venv/
# ENV/
# .pytest_cache/
# .coverage
# htmlcov/
# ===============================================================================